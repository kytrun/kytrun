name: Update README from RSS

on:
  issues:
    types: [labeled]

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: JasonEtco/rss-to-readme@v1
        with:
          feed-url: https://tangkaichuan.cn/atom.xml
          readme-section: feed